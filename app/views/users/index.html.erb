
<% if current_user.admin? %>
<div class="search-form">
<% provide(:title, '一覧') %>
<h1>ユーザ一覧</h1>
<%= label_tag :name, "ユーザー名検索" %>

<%= form_tag(users_path, method: "get") do %>
<div class="form-group">
<%= submit_tag "検索", class: "btn btn-primary btn-search" %>
<%= text_field_tag :name, params[:name], class: "form-control index_tag", placeholder: "キーワードを入力..." %>
<% end %>
  </div>
</form>
<%= will_paginate %>



<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>


  <% end %>